# Base system packages for the SOLIS image.
# Gaming/multimedia packages (RetroArch deps, OpenAL, VLC, espeak, etc.) have
# been removed.  SOLIS is cross-compiled and deployed as a binary, so no
# toolchain ships on-device (apt install rustc if ever needed).

# --- Base system / filesystem / build ---
bsdextrautils
btrfs-progs
ccache
cloud-guest-utils
console-setup
device-tree-compiler
dialog
dos2unix
exfatprogs
liblzma5
libstdc++6
libudev1
netcat-openbsd
ntfs-3g
p7zip-full
psmisc
pv
python3
python3-evdev
python3-pip
python3-setuptools
python3-wheel
rsync
systemd-timesyncd
tar
unzip
wget
xfsprogs
xxd
zip

# --- Display / input ---
brightnessctl
libxkbcommon0

# --- WiFi ---
iw
rfkill
firmware-mediatek

# --- Audio / MIDI (SOLIS) ---
alsa-utils
libasound2t64
# SOLIS's audio path is the sdl2 crate (audio subsystem only, ALSA backend)
libsdl2-2.0-0
